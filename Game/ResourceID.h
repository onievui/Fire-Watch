#pragma once



enum TextureID {
	TEXTURE_LOGO,
	TEXTURE_LOGO2,
	TEXTURE_TITLE,
	TEXTURE_MAPCHIP,
	TEXTURE_PLAYER,
	TEXTURE_ENEMY1,
	TEXTURE_TENT,
	TEXTURE_FIRE,
	TEXTURE_CROSSBOW,
	TEXTURE_LIGHT1,
	TEXTURE_FLASHLIGHT,
	TEXTURE_ARROW,
	TEXTURE_NUM
};

enum SoundID {
	SOUND_REFLECT,
	SOUND_DESTROY,
	SOUND_NUM
};

enum MusicID {
	MUSIC_BGM,
	MUSIC_NUM
};

enum MovieID {
	MOVIE_NULL,
	MOVIE_NUM
};

enum FontID {
	FONT_NULL,
	FONT_NUM
};